# Optional Waymo/TensorFlow stack for the original legacy evaluator.
#
# This is not required for the short learning probe, which disables the legacy
# validation evaluator and compares checkpoints with the repo's head-to-head
# tooling instead.
tensorflow==2.12.0
tensorflow-addons==0.23.0
tensorflow-datasets==4.9.9
tensorflow-probability==0.19.0
waymo-open-dataset-tf-2-12-0==1.6.4
